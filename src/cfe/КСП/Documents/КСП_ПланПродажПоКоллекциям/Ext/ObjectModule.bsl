
Процедура ОбработкаПроведения(Отказ, Режим)

	// регистр КСП_ПланыПродаж
	Движения.КСП_ПланыПродаж.Записывать = Истина;
	Для Каждого ТекСтрокаПлан Из План Цикл
		Движение = Движения.КСП_ПланыПродаж.Добавить();
		Движение.Период = Дата;
		Движение.Клиент = ТекСтрокаПлан.Клиент;
		Движение.Коллекция = Коллекция;
		Движение.План = ТекСтрокаПлан.ПланПродаж;
	КонецЦикла;

КонецПроцедуры
