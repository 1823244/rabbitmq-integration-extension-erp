
&После("ОбработкаПроведения")
Процедура КСП_ОбработкаПроведения(Отказ, РежимПроведения)

	//Движения.КСП_ВыставленныеСчетаПоСтрокамЗаказа_РН.Записывать = Истина;
	//Движение =  Движения.КСП_ВыставленныеСчетаПоСтрокамЗаказа_РН.Добавить();  	
	//Движение.Регистратор = ЭтотОбъект.Ссылка;
	//СчетНаОплатуКлиентуЛокализация.КСП_ОбработкаПроведения(ЭтотОбъект, Отказ, РежимПроведения);
КонецПроцедуры
